$(document).ready(function () {
    $(".seat").click(function () {
        var seatId = $(this).data("seat-id");
        // Handle the click event here, e.g., redirect to a new page with the seatId
    });
});